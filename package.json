{
  "name": "lorcana-collector",
  "version": "0.0.1",
  "private": true,
  "description": "Disney Lorcana trading card collection tracker",
  "scripts": {
    "build": "pnpm -r build",
    "dev:api": "pnpm --filter @lorcana/api dev",
    "dev:web": "pnpm --filter @lorcana/web dev",
    "dev": "pnpm run dev:api & pnpm run dev:web"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["bcrypt", "@nestjs/core", "esbuild"]
  }
}
